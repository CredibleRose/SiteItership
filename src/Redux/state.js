import ChelikList from "../Pages/Components/Cheliks/ChelikList";
import axios from "axios";

/*const axios = require ('axios').default;
axios
    .get('https://stoplight.io/mocks/kode-education/trainee-test/25143926/users', {headers: {
        'Contetnt-type' : 'application/json',
            'Prefer': 'code=200, example=success'
    }})
    .then((response => {
        console.log(response.data.items)
        const CheliksData = response

    }));
export default axios*/
